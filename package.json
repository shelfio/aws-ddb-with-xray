{
  "name": "@shelf/aws-ddb-with-xray",
  "version": "2.4.1",
  "description": "AWS DynamoDB Document Client initialized with X-Ray",
  "keywords": [
    "aws sdk",
    "document client",
    "dynamodb",
    "x-ray",
    "xray"
  ],
  "repository": "shelfio/aws-ddb-with-xray",
  "license": "MIT",
  "author": {
    "name": "Vlad Holubiev",
    "email": "vlad@shelf.io",
    "url": "shelf.io"
  },
  "main": "lib",
  "types": "lib/index.d.ts",
  "files": [
    "lib"
  ],
  "scripts": {
    "build": "rm -rf lib/ && pnpm build:types && babel src --out-dir lib --ignore '**/*.test.ts' --extensions '.ts' && find ./lib -name '*.test.d.ts' -delete",
    "build:types": "tsc --emitDeclarationOnly --declaration --isolatedModules false --declarationDir lib",
    "coverage": "jest --coverage",
    "lint": "eslint . --ext .js,.ts,.json --fix",
    "lint:ci": "eslint . --ext .js,.ts,.json",
    "prepack": "pnpm build",
    "test": "jest src --runInBand --forceExit",
    "type-check": "tsc --noEmit",
    "type-check:watch": "npm run type-check -- --watch"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged",
      "post-commit": "git update-index --again"
    }
  },
  "babel": {
    "extends": "@shelf/babel-config/backend"
  },
  "prettier": "@shelf/prettier-config",
  "jest": {
    "preset": "@shelf/jest-dynamodb"
  },
  "dependencies": {
    "@aws/dynamodb-expressions": "0.7.3",
    "aws-xray-sdk-core": "3.12.0"
  },
  "devDependencies": {
    "@babel/cli": "7.28.6",
    "@babel/core": "7.29.0",
    "@shelf/babel-config": "3.0.0",
    "@shelf/eslint-config": "5.4.1",
    "@shelf/jest-dynamodb": "4.0.2",
    "@shelf/prettier-config": "1.0.0",
    "@shelf/tsconfig": "0.1.0",
    "@types/jest": "30.0.0",
    "@types/node": "25.2.1",
    "eslint": "9.39.2",
    "husky": "9.1.7",
    "jest": "30.2.0",
    "lint-staged": "16.2.7",
    "prettier": "3.8.1",
    "typescript": "5.9.3"
  },
  "peerDependencies": {
    "@aws-sdk/client-dynamodb": "3.x.x",
    "@aws-sdk/lib-dynamodb": "3.x.x"
  },
  "pnpm": {
    "overrides": {
      "lodash": "4.18.1"
    }
  },
  "engines": {
    "node": ">=16"
  },
  "publishConfig": {
    "access": "public"
  },
  "lint-staged": {
    "*.{html,json,md,yml}": [
      "prettier --write --ignore-path=./.eslintignore"
    ],
    "*.{ts,js}": [
      "eslint --fix"
    ]
  }
}
